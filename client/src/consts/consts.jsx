const advertisementsURL = "advertisements";
const usersURL = "/users";
const loginURL = "/login";
const logoutURL = "/logout";
const createURL = "create";
const dashboardURL = "/dashboard";
const homeURL = "/";
const categoriesURL = `${advertisementsURL}/categories`;
const tagsURL = `${advertisementsURL}/tags`;

export default { advertisementsURL, loginURL,logoutURL, createURL, dashboardURL, homeURL, usersURL, categoriesURL, tagsURL};
